#include <bits/stdc++.h>
#define rep(i, n, m) for (int i = (n); i < (m); ++i)
using namespace std;
using ll = long long;
using Graph = vector<vector<int>>;
using Cubes = vector<vector<vector<int>>>;

int main() {
  Cubes cubes(4, vector<vector<int>>(4, vector<int>(4, 0)));
  return 0;
}
